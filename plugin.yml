name: FactionMobs
main: com.gmail.scyntrus.fmob.FactionMobs
author: Scyntrus
version: 0.1
depends: [Factions]
commands:
    fm:
        description: spawn something
        usage: "/fm [command] [parameters]"
permissions:
    fmob.spawn:
        description: Allows you to spawn a faction mob
        default: op
    